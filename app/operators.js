// Holds code for all the operators

// Operators are triggered when an event happens
// When an operator is running, we don't stop until completion
// Make 'normal' mode an operator?

function Move() {
    //stub
}